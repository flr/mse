% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ind.R
\name{cpues.ind}
\alias{cpues.ind}
\title{Compute a CPUE-based indicator for use in empirical harvest control rules}
\usage{
cpues.ind(
  stk,
  idx,
  refyrs,
  nyears = 4,
  indices = names(idx),
  combine = c("mean", "zscore", "smooth"),
  weights = rep(1, length(indices)),
  enp.mult = 0.2,
  args,
  tracking
)
}
\arguments{
\item{stk}{An \code{FLStock} object, returned unmodified in the output list.}

\item{idx}{An \code{FLIndices} object with the CPUE or survey indices from
which the indicator is computed.}

\item{refyrs}{Reference years used to rescale (\code{"mean"}, \code{"smooth"}) or
standardize (\code{"zscore"}) each index.}

\item{nyears}{Number of recent years, ending in \code{dy}, over which the
indicator is computed. \code{numeric}, defaults to 4.}

\item{indices}{Names of the elements of \code{idx} to use. Defaults to
\code{names(idx)}, i.e. all indices in \code{idx}.}

\item{combine}{Method used to combine the rescaled indices into a single
indicator, one of \code{"mean"}, \code{"zscore"} or \code{"smooth"}.}

\item{weights}{Weights applied to each index, used by all three \code{combine}
methods, passed on to \code{weighted.mean}. Defaults to equal weights,
\code{numeric} of length \code{length(indices)}.}

\item{enp.mult}{Smoothing parameter passed to \code{smooth_index} as
\code{enp.mult}, only used when \code{combine = "smooth"}. Defaults to 0.2.}

\item{args}{A \code{list} of MSE loop arguments, e.g. \code{ay} (assessment year)
and \code{dy} (last data year), unpacked using \code{mse::spread}.}

\item{tracking}{An \code{mse} tracking object (\code{FLQuant}) used to store the
computed indicator(s) for later inspection.}
}
\value{
A \code{list} with three elements:
\item{stk}{The input \code{FLStock}, unchanged.}
\item{ind}{An \code{FLQuants} with the combined indicator(s): \code{ind}, \code{mean}
and \code{slope} for \code{combine = "mean"}; \code{mean} only for \code{combine = "zscore"}
or \code{combine = "smooth"}.}
\item{tracking}{The updated tracking object.}
}
\description{
\code{cpues.ind} computes a relative abundance indicator from one or more CPUE
or survey indices (\code{idx}), to be used by empirical indicator (\code{ind})
modules in an \code{mse} Management Strategy Evaluation loop. Indices are first
restricted to a recent window of \code{nyears}, rescaled by dividing by their
mean over a set of reference years (\code{refyrs}), and then combined across
indices, using \code{weights}, with one of three methods: a weighted rescaled
mean (\code{"mean"}), a weighted mean of standardized z-scores on the log scale
(\code{"zscore"}), or a weighted mean of smoothed, rescaled indices
(\code{"smooth"}). For \code{combine = "mean"}, the function additionally computes
the mean level and log-linear slope of the combined indicator over the
recent window.
}
\details{
\code{cpues.ind} is designed to be called inside an \code{mse} indicator (\code{ind})
module, and expects \code{args} to contain at least the current assessment
year (\code{ay}) and last data year (\code{dy}), unpacked internally via
\code{mse::spread}. The mean (and, for \code{combine = "mean"}, the slope) of the
indicator is stored in \code{tracking} under \code{"mean.ind"} and \code{"slope.ind"}.

Combination across indices is done by the \code{weighted.mean} method for
\code{FLQuants}, which by default excludes \code{NA} values from both the weighted
sum and the sum of weights, so indices missing in a given year do not
bias the result towards zero or dilute the contribution of the indices
that do have data.
}
\examples{
\dontrun{
data(ple4)
data(ple4.indices)

args <- list(ay=2017, dy=2017)
tracking <- FLQuant()

out <- cpues.ind(stk=ple4, idx=ple4.indices, refyrs=2005:2010,
  nyears=4, combine="mean", weights=c(1, 2), args=args,
  tracking=tracking)
}
}
\seealso{
\link[mse]{mp}, \link{weighted.mean}, \link{smooth_index},
\link{zscore}
}
\author{
Iago Mosqueira (WMR), FLR Team.
}
\keyword{methods}
