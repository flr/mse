% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mp.R
\name{mps}
\alias{mps}
\title{mps runs multiple Management Procedures with varying module arguments}
\usage{
mps(
  om,
  oem = NULL,
  iem = NULL,
  control = ctrl,
  ctrl = control,
  args,
  statistics = mse::statistics,
  metrics = NULL,
  type = character(1),
  names = NULL,
  perf = FALSE,
  ...
)
}
\arguments{
\item{om}{The operating model, an object of class \emph{FLom} or \emph{FLombf}.}

\item{oem}{The observation error model, an object of class \emph{FLoem}.
Optional, defaults to \code{perfect.oem()} if not provided.}

\item{iem}{The implementation error model, an object of class \emph{FLiem}.
Optional.}

\item{control}{A control structure for the MP runs, an object of class \emph{mpCtrl}.}

\item{ctrl}{Alias for \code{control}. If both are provided, \code{control} takes precedence.}

\item{args}{MSE arguments, \emph{list}. Must contain 'iy' (intermediate/starting year).
Other standard elements: 'fy' (final year), 'y0' (first data year), 'nsqy',
'data_lag', 'management_lag', 'frq' (frequency), 'vy' (year vector).}

\item{statistics}{Optional. A list or vector of statistic names to compute
performance metrics. If provided, \code{perf} is automatically set to \code{TRUE}.}

\item{metrics}{Optional. A list or vector of metric names for performance
evaluation.}

\item{type}{Optional. A label of the type of MP being run to use in the
perfoamnce table.}

\item{names}{Optional. A character vector of custom names for each MP run.
If not provided, names are auto-generated from module arguments.
If a single name is provided, it is prefixed to auto-generated names.}

\item{perf}{Logical. If \code{TRUE}, compute and return performance metrics.
Defaults to \code{TRUE} if \code{statistics} is provided, \code{FALSE} otherwise.}

\item{...}{Named module arguments to vary. Only one module name is allowed.
Arguments are passed as lists or vectors and recycled to equal length.
Example: \code{hcr = list(lim = c(0.1, 0.2), trigger = c(40000, 50000))}.}

\item{parallel}{Logical or numeric. If \code{TRUE} (default), use all available
workers set via \code{plan()}. If numeric, use that many cores.}
}
\value{
If \code{perf=FALSE}, default: An object of class \emph{FLmses} containing the results
of each MP run as objects of class \emph{FLmse}.
If \code{perf=TRUE}: A \code{data.table} combining performance metrics from all MP runs,
with an 'mp' column identifying each run.
}
\description{
This function runs multiple variations of a Management Procedure (MP) by
systematically varying the values of one or multiple arguments for a single
control module. Each variation is run as a complete MP simulation under different
module parameter sets.
}
\details{
The function allows you to conduct sensitivity analyses or parameter sweeps
by running multiple MPs where a single module's arguments are varied across
a range of values. All other modules remain constant across runs.

If no module arguments are provided via \code{...}, a single \code{mp()} run is executed
and returned as an \code{FLmses} object with one element named "RUN".

Parallel execution is supported when a future plan has been set up via
\code{plan()}. The number of MPs to run is distributed across available workers.
Progress reporting uses the \code{progressr} package if a global handler is configured.

When \code{perf=TRUE} only the performance metrics computed for each MP run are returned
as a single combined performance \code{data.table}.
}
\examples{
\dontrun{
# Run sensitivity analysis on HCR trigger points
data(plesim)
control <- mpCtrl(list(
  est = mseCtrl(method=perfect.sa),
  hcr = mseCtrl(method=hockeystick.hcr, args=list(lim=0,
  trigger=10000, target=0.18))))

# Vary HCR trigger across values
results <- mps(om, oem = oem, ctrl = control, args = list(iy = 2021, fy = 2034),
  hcr = list(trigger = c(4000, 8000, 12000)))

# With performance metrics
results_perf <- mps(om, oem = oem, ctrl = control, args = list(iy = 2021, fy = 2034),
  hcr = list(trigger = c(4000, 8000, 12000)),
  statistics = statistics)
}
}
