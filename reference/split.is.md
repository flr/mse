# Split a biol target across fleets by setting relative 'quant' proportions

Split a biol target across fleets by setting relative 'quant'
proportions

## Usage

``` r
split.is(stk, ctrl, split, quant = unique(ctrl$quant)[1], args, tracking)
```

## Examples

``` r
data(ple4)
```
